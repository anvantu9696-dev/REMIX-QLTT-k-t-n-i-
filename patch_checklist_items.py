import sqlite3

# Try to find the sqlite database, wait we are using sql.js maybe?
# "import initSqlJs, { Database, SqlValue } from 'sql.js';"
# Actually, the sqlite file is probably saved to a file, let's check `server/db.ts` to see where it saves the db.
